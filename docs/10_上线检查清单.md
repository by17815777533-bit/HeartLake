# 上线检查清单

本文只写当前版本上线前必须确认的事项。

## 1. 代码与文档

- GitHub `main` 已同步
- `README.md` 和 `docs/` 已按当前现状重写
- 架构图、API 清单、压测结果、部署手册都已更新

## 2. 构建检查

```bash
./scripts/verify-2c2g.sh
cd admin && npm run lint && npm run build
cd backend && cmake --build build-2c2g -j2
cd frontend && flutter analyze
```

## 3. 功能检查

- 匿名登录可用
- 账号信息、隐私设置可读写
- 石头、涟漪、纸船链路可用
- 好友、临时好友、私信链路可用
- 情绪日历、热力图、推荐、高级推荐可用
- 湖神、安全港、咨询、VIP、EdgeAI 可用
- 管理端 Dashboard、用户、内容、审核、举报、日志可用

## 4. 现网检查

```bash
curl -fsS http://121.41.195.165/api/health
curl -fsS http://121.41.195.165/healthz
curl -I -fsS http://121.41.195.165/admin/
```

- WebSocket `ws://121.41.195.165/ws/broadcast` 可鉴权连接
- backend / admin / postgres / redis 容器全部 healthy

## 5. 性能检查

- HTTP soak 基线：`406572` 请求，整体 `1330.91 rps`
- 湖面列表 `p95 16.61ms`
- 账号信息 `p95 16.32ms`
- 高级共鸣 `p95 19.45ms`
- 涟漪写链 `p95 17.77ms`
- WebSocket `120` 并发连接全部成功，握手平均 `42.67ms`
- 压测窗口内无超过 `20ms` 的慢 SQL

## 6. 发布物检查

- APK 路径：`frontend/build/app/outputs/flutter-apk/app-release.apk`
- 当前 APK SHA-256：`9654d5facf294ab1c0d21e6ce6f73728d346977994fe911a23be1de02553ac31`

## 7. 部署信息

- 云端目录：`/root/HeartLake`
- 公网入口：`http://121.41.195.165`
- API：`http://121.41.195.165/api`
- 管理后台：`http://121.41.195.165/admin/`
- WebSocket：`ws://121.41.195.165/ws/broadcast`
