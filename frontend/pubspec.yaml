name: heart_lake
description: 心湖 · 涟漪共鸣 — 匿名情绪表达与心理关怀平台
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  uuid: ^4.3.3
  web_socket_channel: ^2.4.0
  image_picker: ^1.0.7
  url_launcher: ^6.2.5
  flutter_secure_storage: ^9.0.0
  cryptography: ^2.7.0
  crypto: ^3.0.0
  get_it: ^7.6.7
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/models/
